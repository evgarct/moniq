import SwiftUI

/// Placeholder — real content lands once `docs/features/accounts.json` is
/// picked from the PostHog-ranked feature slice. See docs/ios-swift-reference.md.
struct BalanceView: View {
    var body: some View {
        FeaturePlaceholderView(title: "Balance", systemImage: "wallet.pass")
    }
}
